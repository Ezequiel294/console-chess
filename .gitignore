# Ignore everything
/*

# Except for these files...
!.gitignore
!README.md
!Makefile
!VERSION
!src
!openspec

# Build output. Already covered by the /* above; listed so the intent is
# obvious and so it stays ignored if the whitelist is ever relaxed.
build/
console-chess
console-chess-debug
game_save.bin
